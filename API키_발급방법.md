# 유튜브 API 키 발급 방법 (무료, 약 5분)

가장 쉬운 방법: 이 폴더의 **`API키_등록.command`를 더블클릭**하세요. 페이지를 순서대로 열어주고, 키를 붙여넣으면 작동하는지 확인한 뒤 저장까지 해줍니다.
(처음 실행할 때 "확인되지 않은 개발자" 경고가 나오면: 파일 우클릭 → [열기] → [열기])

## 순서
1. **프로젝트 만들기** — https://console.cloud.google.com/projectcreate
   - 평소 쓰는 구글 계정으로 로그인
   - 프로젝트 이름 `channel-dashboard` → [만들기]
   - 처음이면 국가 선택·서비스 약관 동의 화면이 먼저 나옴. **결제(카드) 정보는 필요 없음**
2. **YouTube Data API v3 켜기** — https://console.cloud.google.com/apis/library/youtube.googleapis.com
   - 화면 위쪽 프로젝트가 `channel-dashboard`인지 확인
   - 파란 **[사용]** 버튼
3. **API 키 만들기** — https://console.cloud.google.com/apis/credentials
   - [+ 사용자 인증 정보 만들기] → **[API 키]** → [복사]
   - (권장) 만들어진 키 이름 클릭 → API 제한사항 **[키 제한]** → `YouTube Data API v3`만 체크 → [저장]
     → 키가 새어나가도 유튜브 조회 말고는 못 씀
4. **등록** — `API키_등록.command` 실행 → 키 붙여넣기 → "✔ 성공"이 나오면 끝

## 주의
- 키를 **채팅에 붙여넣지 마세요.** 등록 도우미가 이 폴더의 `.env`에만 저장합니다(나만 읽을 수 있게 권한 설정).
- 무료 한도: 하루 10,000 단위. 4채널을 1시간마다 확인해도 한도의 일부만 씀.
- 키를 잃어버리거나 새어나갔으면 3번 페이지에서 키를 삭제하고 새로 만들면 됩니다.
